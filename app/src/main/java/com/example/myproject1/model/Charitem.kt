package com.example.myproject1.model

class Charitem {

    var icons:Int ? = 0
    var alpha:String ? = null

    constructor(icons: Int?, alpha: String?) {
        this.icons = icons
        this.alpha = alpha
    }
}